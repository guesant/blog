namespace Portfolio.Blazor.UI.Foundations;

/// <summary>Visual treatment of a SiteList.</summary>
public enum SiteListVariant
{
    /// <summary>No chrome, just spacing.</summary>
    Plain,

    /// <summary>Items on the muted surface.</summary>
    Surface,

    /// <summary>Items in a bordered box.</summary>
    Boxed,

    /// <summary>Numbered steps.</summary>
    Steps,
}
