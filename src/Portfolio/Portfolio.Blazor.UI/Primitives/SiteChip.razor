@if (!string.IsNullOrWhiteSpace(Href))
{
    <a href="@Href" title="@Title" class="@LinkClass" data-slot="chip">@Label</a>
}
else
{
    <span class="@SpanClass" title="@Title" data-slot="chip">@Label</span>
}
