# IMPORTANT: pinned so the project name (and therefore the named volumes) does
# not depend on the checkout directory's name, which differs per machine.
name: portfolio

services:
  resume-cache-init:
    image: mcr.microsoft.com/dotnet/sdk@sha256:e1fc6e423f543119c406d24e2e687d67c569f18f04a37a8b0005d80ad0dcee80
    user: "0:0"
    volumes:
      - resume-pdf-cache:/data/resume-cache
    command: sh -lc 'mkdir -p /data/resume-cache && chown -R 1000:1000 /data/resume-cache'
    restart: "no"
  db-backups-init:
    image: mcr.microsoft.com/dotnet/sdk@sha256:e1fc6e423f543119c406d24e2e687d67c569f18f04a37a8b0005d80ad0dcee80
    user: "0:0"
    volumes:
      - db-backups:/data/db-backups
    command: sh -lc 'mkdir -p /data/db-backups && chown -R 1000:1000 /data/db-backups'
    restart: "no"
  web:
    image: mcr.microsoft.com/dotnet/sdk@sha256:e1fc6e423f543119c406d24e2e687d67c569f18f04a37a8b0005d80ad0dcee80
    user: "${PORTFOLIO_CONTAINER_UID:-1000}:${PORTFOLIO_CONTAINER_GID:-1000}"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    working_dir: /src
    volumes:
      - .:/src
      # A single-file bind mount can't hold the -wal/-shm sidecar files SQLite
      # needs once the DB is in WAL mode (required so the admin panel's writes
      # don't block the public site's concurrent reads) — confirmed live via
      # "SQLite Error 14: unable to open database file" until this was changed
      # to mount the containing directory instead of just the one file.
      - type: bind
        source: ${PORTFOLIO_SQLITE_HOST_DIR:-./data}
        target: /data/db
        bind:
          create_host_path: false
      - type: bind
        source: ${PORTFOLIO_PUBLIC_ASSET_PATH:-./data/public}
        target: /data/public
        read_only: true
        bind:
          create_host_path: false
      - resume-pdf-cache:/data/resume-cache
      - nuget-cache:/tmp/nuget-packages
      - db-backups:/data/db-backups
    tmpfs:
      - /tmp:uid=${PORTFOLIO_CONTAINER_UID:-1000},gid=${PORTFOLIO_CONTAINER_GID:-1000},mode=1777
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://0.0.0.0:8080
      PORTFOLIO_SQLITE_PATH: /data/db/portfolio.sqlite
      PORTFOLIO_PUBLIC_ASSET_ROOT: /data/public
      PORTFOLIO_RESUME_PDF_ROOT: /data/resume-cache
      PORTFOLIO_DB_BACKUP_ROOT: /data/db-backups
      TECTONIC_BUNDLE: https://data1.fullyjustified.net/tlextras-2022.0r0.tar
      TECTONIC_ONLY_CACHED: "true"
      HOME: /data/resume-cache
      XDG_CACHE_HOME: /data/resume-cache/.cache
      PUBLIC_SITE_BASE_URL: http://localhost:8080
      DOTNET_CLI_TELEMETRY_OPTOUT: "1"
      DOTNET_NOLOGO: "true"
      DOTNET_CLI_HOME: /tmp/dotnet
      NUGET_PACKAGES: /tmp/nuget-packages
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD-SHELL", "curl --fail --silent http://127.0.0.1:8080/health >/dev/null || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 15s
    command: sh ./tools/scripts/run-with-tectonic.sh
    depends_on:
      resume-cache-init:
        condition: service_completed_successfully
      db-backups-init:
        condition: service_completed_successfully

volumes:
  nuget-cache:
  resume-pdf-cache:
  db-backups:
