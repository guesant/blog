@inherits LocalizedComponentBase
@page "/tools/markdown-to-html"
@page "/pt-BR/tools/markdown-to-html"
@using Portfolio.Blazor.Core

<ToolPage Title="@Title" Description="@Description" CanonicalPath="@CanonicalPath">
    <SiteToolForm Action="@Action" SubmitLabel="@ConvertLabel" AriaLabel="@MarkdownInputLabel">
        <SiteFormField Label="@MarkdownInputLabel" For="markdown-html-input">
            <SiteTextAreaInput Id="markdown-html-input"
                Name="input"
                Rows="10"
                spellcheck="false"
                Value="@Input"
                ValueChanged="@(v => Input = v)" />
        </SiteFormField>
    </SiteToolForm>
    <SiteLiveRegion Politeness="SiteLivePoliteness.Polite">
        <SiteCard Class="site-block" Label="@PreviewLabel">
            <SiteHtmlPreview Class="tool-markdown-preview">@((MarkupString)Html)</SiteHtmlPreview>
        </SiteCard>
        <SiteOutputField Id="markdown-html-output"
            Label="@HtmlOutputLabel"
            Value="@Html"
            Multiline="true"
            CopyLabel="@CopyLabel" />
    </SiteLiveRegion>
</ToolPage>
