<i class="site-icon @Class @IconClasses @IconSizeClass"
    data-slot="icon"
    role="@(string.IsNullOrWhiteSpace(AriaLabel) ? null : "img")"
    aria-label="@(string.IsNullOrWhiteSpace(AriaLabel) ? null : AriaLabel)"
    aria-hidden="@(string.IsNullOrWhiteSpace(AriaLabel) ? "true" : null)"></i>
