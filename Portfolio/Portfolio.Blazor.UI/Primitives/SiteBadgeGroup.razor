<div class="@RootClass"
    data-slot="badge-group"
    role="@(string.IsNullOrWhiteSpace(AriaLabel) ? null : "group")"
    aria-label="@AriaLabel"
    @attributes="AdditionalAttributes">
    @ChildContent
</div>
