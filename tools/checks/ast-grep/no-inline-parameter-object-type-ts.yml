id: no-inline-parameter-object-type-ts
language: TypeScript
severity: error
message: Extraia o tipo inline do parâmetro para um type ou interface nomeado.
rule:
  kind: object_type
  inside:
    any:
      - kind: required_parameter
      - kind: optional_parameter
    stopBy: end
