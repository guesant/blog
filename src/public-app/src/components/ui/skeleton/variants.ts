import type { SxProps, Theme } from '@mui/material/styles';

export const skeletonVariants: Record<string, SxProps<Theme>> = {
  progressiveItem: {
    height: 'var(--site-space-12)',
    width: '100%',
  },
};
