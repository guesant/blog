import type { SxProps, Theme } from '@mui/material/styles';

export const pageHeaderLayout: SxProps<Theme> = {
  display: 'grid',
  rowGap: 'var(--site-space-3)',
  paddingBlock: 'var(--site-space-3)',
};
