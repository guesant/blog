import type { IllustrationTranslator } from '@/i18n/compat-support';
import type { IllustrationAccent } from './types';

export type ArchitectureDiagramOptions = {
  accent: IllustrationAccent;
  t: IllustrationTranslator;
};
