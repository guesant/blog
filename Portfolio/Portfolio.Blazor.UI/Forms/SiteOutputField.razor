<SiteFormField Label="@Label" For="@Id">
    @if (Multiline)
    {
        <SiteTextAreaInput Id="@Id" Value="@Value" readonly />
    }
    else
    {
        <SiteInput Id="@Id" Type="text" Value="@Value" readonly />
    }
    @if (Copyable)
    {
        <SiteCopyButton Value="@Value" Label="@CopyLabel" />
    }
</SiteFormField>
